[package]
name = "router-client"
version = "0.1.0"
authors = ["de-user"]
build = "build.rs"

[build-dependencies]
serde_codegen = "0.8"

[dependencies]
error-chain = {version = "0.7"}
serde = {version = "0.8"}
serde_json = {version = "0.8"}
#hyper = {version = "0.9", features = []}
nickel = {version = "0.9"}
chrono = {version = "0.2", features = ["serde"]}
clap = {version = "2.19"}
